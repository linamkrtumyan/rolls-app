## Rolls shop app (React Native Expo + Nativewind(Tailwind) + Typescript + Redux toolkit)

1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ```bash
    npx expo start
   ```


